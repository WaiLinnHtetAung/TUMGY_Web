export type ContainerType = {
    children: React.ReactNode,
    className?: string,
    isNav?: boolean
}

export type EmailType = {
    name: string,
    email: string,
    subject: string,
    message: string
}