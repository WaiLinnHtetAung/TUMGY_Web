export type announcementTypes = {
    announcements: Array<any>,
    error: string | boolean,
    isLoading: string | boolean
}

export type newsEventsTypes = {
    news_events: Array<any>,
    error: string | boolean,
    isLoading: string | boolean
}

export type rectorMessageTypes = {
    rector_message: any,
    error: string | boolean,
    isLoading: string | boolean
}