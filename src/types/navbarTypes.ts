export type DepartmentDropdownTypes = {
    sub_route_name: string;
    departments: Array<any>;
    error: string | boolean;
    isLoading: string | boolean;
}

export type ThesisDropdownTypes = {
    sub_route_name: string;
    departments: Array<any>;
    error: string | boolean;
    isLoading: string | boolean;
}